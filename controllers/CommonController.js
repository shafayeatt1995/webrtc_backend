const controller = {
  // async fetchNavbar(req, res) {
  //   try {
  //     return res.json({ districts: true });
  //   } catch (error) {
  //     console.error(error);
  //     return res.status(500).json({ message });
  //   }
  // },
};

module.exports = controller;
